# Toolbox vX.Y.Z

## What's new

- (user-facing change)
- (user-facing change)

## Install

Download from [GitHub Releases](https://github.com/MerlijndeVos/Toolbox/releases/tag/vX.Y.Z):

| Platform | File |
|----------|------|
| Windows | `Toolbox-Setup-X.Y.Z-win64.exe` |
| macOS | `Toolbox-X.Y.Z-macos.dmg` |
| Linux | `Toolbox-X.Y.Z-linux-{arch}.AppImage` |

Installers are **unsigned**. Windows SmartScreen and macOS Gatekeeper may ask you to confirm once (“Run anyway” / “Open”).

**ffmpeg** is not bundled in the installer. On first launch the app downloads a one-time essentials build into your user data folder.

## Updating from vPREV

Use **Settings → Updates → Update to vX.Y.Z** in the desktop app, or download the new installer for your OS from the table above.
